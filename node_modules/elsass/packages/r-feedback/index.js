import RFeedback from './r-feedback.vue'
// 接口封装调用
import fetch from '../../src/apis/fetch/index.js'

RFeedback.install = function (Vue) {
  Vue.component(RFeedback.name, RFeedback)
  Vue.prototype.$elsassFetch = (optons, params) => fetch(optons, params)
}

export default RFeedback